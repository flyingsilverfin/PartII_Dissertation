\documentclass{standalone}

\usepackage{pgfplots}
\pgfplotsset{compat=newest}


\definecolor{darkergreen}{RGB}{51, 184, 51}

\begin{document}
\begin{tikzpicture}
\begin{axis}[
	title=Insertion and Removal Memory Growth,
	xlabel={Number of Character Insertions and Removals (1000s)},
	ylabel={Additional Mememory Consumed (MBytes)},
	bar width=10pt,
	xmin=0, xmax=50500,
	ymax=55000000,
	scale=1.25,
	scaled y ticks = real:1000000,
	y tick label style={/pgf/number format/fixed},
	ytick scale label code/.code={},
	scaled x ticks = real:1000,
	x tick label style={/pgf/number format/fixed},
	xtick scale label code/.code={},
	legend pos=north west,
	enlargelimits=false,
	legend cell align=left,
	]

\addplot+ 
 plot[error bars/.cd, y dir = both, y explicit]
 table[x index=1, y index = 2, y error index= 4] {./ot_client_memory.dat_STATS.dat};
 \addlegendentry{ShareJS Client}

\addplot+ 
 plot[error bars/.cd, y dir = both, y explicit]
 table[x index=1, y index = 2, y error index= 4] {./crdt_memory.dat_STATS.dat};
 \addlegendentry{CRDT using Insert/Delete}
 
\addplot+ [black]
  plot[error bars/.cd, y dir = both, y explicit]
  table[x index=1, y index = 2, y error index= 4] {./share_server_memory.dat_STATS.dat};
  \addlegendentry{ShareJS Server}
  
\addplot+ [darkergreen]
  plot[error bars/.cd, y dir = both, y explicit]
  table[x index=1, y index = 2, y error index= 4] {./crdt_memory_undoredo.dat};
  \addlegendentry{CRDT using Undo/Redo}

\end{axis}
\end{tikzpicture}

\end{document}